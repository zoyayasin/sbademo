import { AdjustLengthPipe } from './adjust-length.pipe';

describe('AdjustLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
